# 通用 AI 启动提示词

将下面内容发送给任何能够读取项目文件的 AI：

```text
日常任务先读取 .ai-spec/business/quick-ref.md；只有状态不是 GENERATED，或本次是接入、初始化、完整审计时，才读取 .ai-spec/AI-START.md。
按动态门禁加载最小必要规则、当前开发计划和相关源码。轻量读取不代表可以跳过安全、契约、验收和真实验证；不要覆盖已有 AI 配置或猜测业务规则。
默认使用简体中文，不得因为代码或文档为英文而自行切换语言。
```

如果规范直接位于项目根，将路径改为 `AI-START.md`。
