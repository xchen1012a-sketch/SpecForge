# 通用 AI 规范一键导入提示词

## 使用方式

先把规范母版放到目标项目的 `.ai-spec/`，然后把下面整段提示词发送给任意能够读取项目文件的 AI。

```text
请为当前项目一键接入 `.ai-spec/` 通用 AI 协作规范，并自动完成项目识别、配置生成、适配入口生成、业务规则候选摸底和验证报告。

本指令已授权你在以下范围内自动执行，不需要再次询问：
- 读取项目结构、构建清单、Git 状态、现有文档、测试和非敏感配置；
- 创建 `.ai-spec/` 内缺失的项目配置、分析报告、候选业务规则和项目地图；
- 在目标文件不存在时创建 AI 工具入口和项目级 Skills；
- 运行只读检查、构建命令识别和规范完整性验证。

本指令没有授权你执行以下操作：
- 修改业务代码、数据库、迁移、依赖版本、CI/CD、权限、认证和生产配置；
- 覆盖任何已有文件；
- 读取或输出 `.env`、Token、密码、私钥、云凭证和生产连接信息；
- 执行 git add、commit、push、reset、checkout、stash；
- 调用生产环境、发送真实消息、创建外部 issue 或产生付费资源。

【阶段 0：定位规范和项目】

1. 将 `.ai-spec/AI-START.md` 所在目录定义为 SPEC_ROOT。
2. 将包含业务代码和构建清单的上级目录定义为 PROJECT_ROOT。
3. 如果 `.ai-spec/AI-START.md` 不存在：
   - 检查当前目录是否直接存在 `AI-START.md`；存在则将当前目录作为 SPEC_ROOT；
   - 两处都不存在时，只输出“请先把规范母版复制到项目根 `.ai-spec/`”，然后停止。
4. 读取 `AI-START.md`，后续动作必须遵守其中的安全底线、上下文路由和交付协议。

【阶段 1：只读项目体检】

只读检查以下内容，不修改文件：

1. 项目根目录结构，深度最多 3 层，跳过依赖、构建产物、缓存和大文件目录。
2. 构建清单与命令来源，例如项目实际存在的 manifest、脚本、任务文件和 CI 配置。
3. Git 状态、当前分支、最近提交；如果不是 Git 仓库则明确记录。
4. 已有 AI 配置：CLAUDE.md、AGENTS.md、`.claude/`、`.agents/`、`.cursor/`、`.github/` 以及其它可见入口。
5. 主要代码目录、测试目录、迁移目录、契约目录、文档目录。
6. 现有 `.ai-spec/ai-spec.yaml`、业务规则、契约、Handoff、ADR、RFC 和例外记录。

禁止为了识别项目而执行安装依赖、启动服务、连接数据库或访问外部网络。

【阶段 2：自动分类】

基于证据自动判断：

1. 项目阶段：
   - new：无有效业务代码或只有脚手架；
   - existing：已有稳定业务代码或发布历史，当前无明确进行中改动；
   - in-progress：有未提交改动、功能分支、联调、迁移或未完成 Handoff；
   - 无法确定时按 in-progress 处理。
2. 项目类型：backend、frontend、fullstack、mobile、library-sdk、cli、data-platform、ai-llm 或 generic。
3. 仓库模式：单仓、Monorepo、多仓工作区或非 Git 目录。
4. 关键等级：low、medium、high、critical；没有证据时使用 medium 并标记待确认。
5. 当前 AI 工具：根据可见入口识别；无法识别时使用 generic。

每个判断必须列出证据，不得只给结论。

【阶段 3：生成项目画像】

目标文件：`.ai-spec/ai-spec.yaml`。

1. 文件不存在：根据真实清单生成，未知字段使用 null、空数组或 project-defined，禁止猜测。
2. 文件已存在：不覆盖；生成 `.ai-spec/generated/ai-spec.proposed.yaml`，只列建议补充和差异。
3. 命令字段只能来自项目已有脚本、清单或 CI；未找到就填 null，不编造命令。
4. 项目画像至少包含：项目名称、描述、阶段、类型、关键等级、语言、框架、数据库、包管理器、真实命令、分支模型、质量门禁和 AI 工具。

【阶段 4：选择规范上下文】

1. 根据项目类型选择 `profiles/` 和 `stacks/` 中适用文件。
2. 不删除不适用文件，只在 `.ai-spec/generated/active-context.md` 中记录：
   - 本项目启用哪些规则；
   - 每条规则的来源路径；
   - 哪些规则暂不适用及原因；
   - 发现的规则冲突；
   - 需要人工决定的项目参数。
3. 安全、权限、数据完整性和验证真实性规则不得因项目类型被关闭。

【阶段 5：生成 AI 工具入口】

按照 `.ai-spec/adapters/README.md` 执行：

1. 只在目标入口不存在时创建并渲染项目名称与 `.ai-spec` 路径。
2. 已有入口绝不覆盖，改为生成 `.ai-spec/generated/adapters/<tool>.proposed.md`，列出建议合并内容。
3. 自动发现规则：
   - 发现 Claude Code 环境：准备 Claude 入口和对应项目级 Skills；
   - 发现 Codex 环境：准备 AGENTS 入口和对应项目级 Skills；
   - 发现 Cursor 环境：准备 Cursor 规则入口；
   - 发现 Copilot 环境：准备 Copilot 入口；
   - 其它工具：使用 generic 启动提示词，不因缺少专用适配器停止。
4. 权限配置已存在时只报告差异，不做 allow/deny 并集，不扩大命令权限。

【阶段 6：自动摸底业务规则】

只读扫描以下证据，按可靠度排序：

1. 已执行测试、领域/服务代码、状态机、实体约束、迁移和已批准契约；
2. commit、PR/MR、ADR、RFC 和 Handoff；
3. README、项目文档、注释和非敏感配置。

生成 `.ai-spec/business/business-rules.discovered.md`，禁止覆盖正式 `business-rules.md`。

每条候选规则必须包含：
- 候选规则 ID；
- 规则内容；
- 来源文件与行号、commit 或文档路径；
- 可靠度：高/中/低；
- 状态：已证实/待确认/冲突；
- 冲突来源；
- 建议由哪个业务或技术 Owner 确认。

没有来源的内容不得写成规则；多个来源冲突时不得自行裁决。

【阶段 7：生成项目地图】

生成 `.ai-spec/business/project-map.md`，包含：

1. 一句话项目定位；
2. 核心业务域和对应代码位置；
3. 主要入口能力；
4. 核心实体、状态流转和关系；
5. 外部集成；
6. 权限和数据范围概览；
7. 构建、测试、运行方式；
8. 已知风险、技术债和待确认问题。

只能记录从项目证据得到的信息。

【阶段 8：按项目阶段生成接入结果】

new：
- 生成完整项目画像、active-context、缺失入口、业务候选和项目地图；
- 输出编码前缺失的业务决策和工程基线。

existing：
- 不改变旧代码；
- 生成 `.ai-spec/reports/adoption-plan.md`，按“安全与交付 → 契约与测试 → 技术栈 → Skills 与治理”分阶段列出接入计划；
- 新规则只约束后续新增或主动修改的代码，不要求一次性重构历史代码。

in-progress：
- 优先保护当前工作区；
- 不创建或修改项目根 AI 入口，只生成 proposed 文件；
- 生成 `.ai-spec/reports/current-work-snapshot.md`，记录当前分支、未提交文件、进行中目标、契约、验证和风险；
- 当前阶段结束前不推动全量规范迁移。

【阶段 9：验证】

验证并记录：

1. 所有新建文件都位于授权范围；
2. 没有覆盖已有文件；
3. 业务代码、数据库和依赖文件没有变化；
4. 生成入口不存在未替换占位符；
5. 所有引用路径存在，或已标记为待生成；
6. 项目命令都有真实来源；
7. 候选业务规则都有来源；
8. 没有读取或输出敏感凭证；
9. Git diff 中只包含规范接入文件；
10. 现有测试和构建未被伪造为“已通过”；没有实际运行就必须写“未验证”。

【阶段 10：输出一键导入报告】

同时生成 `.ai-spec/reports/import-report.md`，并在对话中输出摘要：

## AI 规范一键导入报告
- SPEC_ROOT：
- PROJECT_ROOT：
- 项目阶段及证据：
- 项目类型及证据：
- 仓库模式：
- 识别的技术栈：
- 识别的真实命令：
- 新建文件：
- 保留未覆盖文件：
- proposed 合并文件：
- 启用的规范上下文：
- 业务候选规则数量：已证实 / 待确认 / 冲突
- 项目地图路径：
- 未执行的验证：
- 当前风险：
- 需要人工确认的前 5 项：
- 推荐下一条指令：

完成标准：自动分析和安全生成已结束，但不得声称业务规则已经正式批准，也不得声称未运行的构建或测试已经通过。
```

## 导入完成后的推荐指令

```text
请读取 `.ai-spec/reports/import-report.md`、`.ai-spec/business/business-rules.discovered.md` 和 `.ai-spec/business/project-map.md`，只列出需要我确认的冲突与高风险项，不修改业务代码。
```
